spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/reactive_order_monitor
    username: reactive
    password: qwerty
  sql:
    init:
      mode: always
  kafka:
    bootstrap-servers: localhost:9092

management:
  tracing:
    sampling:
      probability: 1.0  # 100% for local practice, 0.1 in prod
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

server:
  port: 8081